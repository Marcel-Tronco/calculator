{
  "name": "calculator",
  "version": "0.0.0",
  "description": "A server exposing an endpoint to a simple calculator.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Marcel Klotz",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "http": "^0.0.1-security"
  },
  "devDependencies": {
    "dotenv": "^10.0.0"
  }
}
